# facial-recognition-app

# before using, run this command for packages:
# "pip install Flask numpy pillow flask-cors"
# "pip install scipy"

# to run the server, use:
# python backend/app.py (preferably run from root)